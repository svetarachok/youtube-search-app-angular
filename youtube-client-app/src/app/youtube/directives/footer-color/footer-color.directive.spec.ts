import { FooterColorDirective } from './footer-color.directive';

describe('FooterColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterColorDirective();
    expect(directive).toBeTruthy();
  });
});
